{
  "name": "generator-flexjs-mxml-component",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "asconfigc && rm -rf ../generators/mxml-component && mkdir -p ../generators && cp -r ./bin/js-release ../generators/mxml-component && cp -r ./templates ../generators/mxml-component"
  }
}