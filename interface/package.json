{
  "name": "generator-flexjs-interface",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "asconfigc && rimraf ../generators/interface && mkdirp ../generators && ncp ./bin/js-release ../generators/interface && ncp ./templates ../generators/interface/templates"
  }
}