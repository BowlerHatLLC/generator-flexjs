{
  "name": "generator-flexjs-interface",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "asconfigc && rm -rf ../generators/interface && mkdir -p ../generators && cp -r ./bin/js-release ../generators/interface && cp -r ./templates ../generators/interface"
  }
}