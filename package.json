{
  "name": "generator-flexjs",
  "version": "0.1.0",
  "description": "Generate an Apache FlexJS application",
  "homepage": "https://nextgenactionscript.com/",
  "files": [
    "generators"
  ],
  "keywords": [
    "yeoman-generator"
  ],
  "dependencies": {
    "yeoman-generator": "^1.0.0",
    "chalk": "^1.1.3",
    "yosay": "^2.0.0"
  },
  "scripts": {
    "build": "npm run build --prefix app && npm run build --prefix mxml && npm run build --prefix class && npm run build --prefix interface",
    "test": "mocha tests",
    "prepublish": "npm run build && npm test"
  },
  "repository": "BowlerHatLLC/generator-flexjs",
  "license": "Apache-2.0",
  "devDependencies": {
    "flexjs": "^0.8.0",
    "mocha": "^3.4.2",
    "yeoman-assert": "^3.0.0",
    "yeoman-test": "^1.6.0"
  }
}
