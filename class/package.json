{
  "name": "generator-flexjs-class",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "asconfigc && rimraf ../generators/class && mkdir -p ../generators && cp -r ./bin/js-release ../generators/class && cp -r ./templates ../generators/class"
  }
}